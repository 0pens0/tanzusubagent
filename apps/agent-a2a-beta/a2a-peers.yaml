# Peer agents reachable from agent-a2a-beta.
# Replace <CF_APPS_DOMAIN> with your foundation's apps domain after cf push.
# Run: cf app agent-a2a-alpha | grep routes
peers:
  - alias: alpha
    url: https://agent-a2a-alpha.<CF_APPS_DOMAIN>
